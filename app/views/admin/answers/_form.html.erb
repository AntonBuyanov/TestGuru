<%= form_with model: [:admin, @question, @answer], local: true do |form| %>
  <%= render 'shared/errors', resource: @answer %>

  <p>
    <%= form.text_area :body, id: :answer_body, autofocus: true, class: 'form-control' %>
  </p>

  <div class="form-check">
    <%= form.label :correct, class: 'form-check-label' do %>
      <%= form.check_box :correct, class: 'form-check-input' %>
      <%= t('admin.answers.correct')  %>
    <% end %>
  </div>

  <p><%= form.submit class: 'btn btn-primary mt-1' %></p>
<% end %>
