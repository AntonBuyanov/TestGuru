<nav class="navbar navbar-success" style="background-color: #F1EFEF;">
  <%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>

  <span class="navbar-text">
    <% if current_user.is_a?(Admin) %>
        <span class="badge rounded-pill text-bg-info"><%= link_to 'AdminPanel', admin_tests_path %></span>
    <% end %>
    <% if user_signed_in? %>
      <%= t('.welcome') %> <b><%= current_user.email %></b> Guru |
      <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('.login'), new_user_session_path %> |
      <%= link_to t('.signup'), new_user_registration_path %>
    <% end %>
  </span>
</nav>
