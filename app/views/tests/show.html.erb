<p>
  <strong>Title:</strong>
  <%= @test.title %>
</p>

<p>
  <strong>Level:</strong>
  <%= @test.level %>
</p>

<p>
  <%= link_to 'New question', new_test_question_path(@test) %>
</p>

<%= link_to 'Edit', edit_test_path(@test) %>
<%= link_to 'Back', tests_path %>

<p>
  <table>
    <tr>
      <th>Questions</th>
      <th colspan="3">Actions</th>
    </tr>
    <% @test.questions.each do |test| %>
    <tr>
      <td><%= test.body%></td>
      <td><%= link_to 'Show', question_path(test) %></td>
      <td><%= link_to 'Edit', edit_question_path(test) %></td>
      <td>
        <%= link_to 'Delete',
                    question_path(test),
                    method: :delete,
                    data: { confirm: 'Are you sure?' }%>
      </td>
    </tr>
    <% end %>
  </table>
</p>

